ALGORITHM Checkpoint_Data
VAR
    Set1,Set2 : ARRAY_OF INTEGER[];
    i,j : INTEGER;
BEGIN
    write("Give two sets");
    read(Set1);
    read(Set1);
    L1=length(Set1);
    L2=length(Set2);
    FOR i FROM 0 TO (L1-1) DO
        FOR j FROM 0 TO (L2-1) DO
            IF (P[i]<>P[j]) THEN
            Sum:=Sum+P[i];
            D[k]:=P[i];
            END_IF
        END_FOR
    END_FOR
END